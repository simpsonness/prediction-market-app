<h1><%= @event.title %></h1>
<p><%= @event.description %></p>

Sell Price
<p><%=  @event.sell_price %></p>

buy Price
<p><%=  @event.price %></p>

<%= image_tag @event.avatar %><br>

<%= link_to 'Order', new_event_order_path(params[:id]) %>

<%= link_to 'Back', events_path %>

<%= link_to "Edit", edit_event_path %>
